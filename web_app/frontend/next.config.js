const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [], // Add external image domains here if needed
  },
  // Uncomment the line below if you have environment-specific configurations
  // env: {
  //   customKey: 'my-value',
  // },
  // Add any other Next.js specific configurations here
};

module.exports = nextConfig;